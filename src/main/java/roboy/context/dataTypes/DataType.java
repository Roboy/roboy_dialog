package roboy.context.dataTypes;

/**
 * Attributes should be able to store data in appropriate data types.
 * Example: coordinates for location data from Vision or Audio.
 *
 * If we want to store data beyond runtime, we could add serialization.
 */
public interface DataType {
}
